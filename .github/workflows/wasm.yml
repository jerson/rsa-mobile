on:
  push:
    tags:
      - "v*"

jobs:
  binding:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '^1.15'
      - name: Build
        run: make wasm
      - uses: montudor/action-zip@v0.1.0
        with:
          args: zip -qq -r wasm_rsa_bridge.zip output/wasm
      - uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: wasm_rsa_bridge.zip
          tag: ${{ github.ref }}
          overwrite: true